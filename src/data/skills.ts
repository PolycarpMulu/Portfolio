import type { Skill } from "@/types";

// TODO: add real skill proficiencies (0–100) for the Recharts chart.
// Left empty for now — assigning levels without real data would be fabrication.
export const skills: Skill[] = [];
